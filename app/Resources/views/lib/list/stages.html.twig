<div class="posts posts-condensed">
    {% for stage in utilisateur.stages %}
        <div class="post ">

            <div class="post-content">
                <h2><a href="blog-detail.html">{{ stage.nom }}</a></h2>
                <p>{{ stage.description[:50] ~ '...'}}</p>
            </div><!-- /.post-content -->
            <div class="post-content">
                <p> se déroule du {{ stage.debut|date("d/m/Y") }} au {{ stage.debut|date("d/m/Y") }}</p>
                <p> s'affiche du {{ stage.affichageDe|date("d/m/Y") }} au {{ stage.affichageJusque|date("d/m/Y")}}</p>
            </div><!-- /.post-content -->
            <div class="post-more">
                <a href={{ path('stage_mise_a_jour') }}>++++</a>
            </div><!-- /.post-date -->
        </div><!-- /.post -->
    {% endfor %}

</div><!-- /.posts -->


{% include 'lib/list/pager.html.twig' %}